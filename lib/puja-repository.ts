import postgres from 'postgres';
export type AdminPuja={id:string;slug:string;name:string;shortDescription:string;fullDescription:string;durationMinutes:number|null;samagriRequired:boolean;samagriAvailable:boolean;onlineAvailable:boolean;templeAvailable:boolean;notes:string;active:boolean;updatedAt:string};
export type PujaInput=Omit<AdminPuja,'id'|'updatedAt'>;
const url=process.env.DATABASE_URL;const sql=url?postgres(url,{max:3,idle_timeout:20,connect_timeout:10}):null;
const fields=sql?.unsafe(`id,slug,name,short_description as "shortDescription",full_description as "fullDescription",duration_minutes as "durationMinutes",samagri_required as "samagriRequired",samagri_available as "samagriAvailable",online_available as "onlineAvailable",temple_available as "templeAvailable",coalesce(notes,'') as notes,active,updated_at::text as "updatedAt"`);
export async function listAdminPujas(){if(!sql||!fields)throw new Error('DATABASE_UNAVAILABLE');return sql<AdminPuja[]>`select ${fields} from pujas where realm='LIVE' and deleted_at is null order by active desc,name`}
export async function createPuja(v:PujaInput){if(!sql||!fields)throw new Error('DATABASE_UNAVAILABLE');const [row]=await sql<AdminPuja[]>`insert into pujas(realm,slug,name,short_description,full_description,duration_minutes,samagri_required,samagri_available,online_available,temple_available,notes,active) values('LIVE',${v.slug},${v.name},${v.shortDescription},${v.fullDescription},${v.durationMinutes},${v.samagriRequired},${v.samagriAvailable},${v.onlineAvailable},${v.templeAvailable},${v.notes},${v.active}) returning ${fields}`;await sql`insert into audit_logs(action,entity_type,entity_id,after_data) values('PUJA_CREATED','Puja',${row.id},${sql.json(row)})`;return row}
export async function updatePuja(id:string,v:PujaInput){if(!sql||!fields)throw new Error('DATABASE_UNAVAILABLE');const [row]=await sql<AdminPuja[]>`update pujas set slug=${v.slug},name=${v.name},short_description=${v.shortDescription},full_description=${v.fullDescription},duration_minutes=${v.durationMinutes},samagri_required=${v.samagriRequired},samagri_available=${v.samagriAvailable},online_available=${v.onlineAvailable},temple_available=${v.templeAvailable},notes=${v.notes},active=${v.active},updated_at=now() where id=${id} and realm='LIVE' and deleted_at is null returning ${fields}`;if(row)await sql`insert into audit_logs(action,entity_type,entity_id,after_data) values('PUJA_UPDATED','Puja',${id},${sql.json(row)})`;return row||null}
