import Link from 'next/link';
import {notFound} from 'next/navigation';
import {Shell} from '../../components';
import {requireRole} from '../../../lib/auth';
import {customerBooking} from '../../../lib/portal-repository';
export default async function BookingStatus({params}:{params:Promise<{id:string}>}){const principal=await requireRole('CUSTOMER'),{id}=await params,booking=await customerBooking(principal.email,id);if(!booking)notFound();return <Shell><main><section className="catalogue-hero"><div className="wrap"><span className="kicker">Booking status</span><h1>{booking.status.replaceAll('_',' ')}</h1><p className="lead">Reference {booking.reference}</p></div></section><section className="section"><div className="wrap detail-shell"><article className="detail-panel"><h2>{booking.serviceSlug.replaceAll('-',' ')}</h2><p>{booking.localDate} at {booking.localTime}</p><p>{booking.providerName} · {booking.locationMode.replaceAll('_',' ')}</p>{booking.venue&&<p>{booking.venue}</p>}{booking.meetingUrl&&<a className="btn" href={booking.meetingUrl} target="_blank" rel="noopener noreferrer">Join online service</a>}</article><aside><div className="sticky-book"><h2>Need a change?</h2><p>Use the secure management link supplied when the booking was created, or contact support.</p><Link className="btn" href="/contact">Contact support</Link></div></aside></div></section></main></Shell>}
