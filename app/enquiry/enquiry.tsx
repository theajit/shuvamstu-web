'use client'; import {useSearchParams} from 'next/navigation'; import {Shell} from '../components'; import {services} from '../data/services'; export default function Enquiry(){const p=useSearchParams();const selected=p.get('service')||'';return <Shell><main><section className="plain-hero compact"><div className="wrap narrow"><span className="kicker">Enquiry</span><h1>Tell us about the occasion.</h1><p className="lead">Share what you know. No public phone number is required to begin.</p></div></section><section className="section"><div className="wrap form-wrap"><form className="enquiry-form" onSubmit={e=>e.preventDefault()}><label>Your name<input name="name" required placeholder="Name"/></label><label>Email<input name="email" type="email" required placeholder="you@example.com"/></label><label>Service<select name="service" defaultValue={selected}><option value="">Choose a service</option>{services.map(s=><option key={s.slug} value={s.slug}>{s.title}</option>)}</select></label><label>Preferred date<input name="date" type="date"/></label><label className="full">Tell us about the occasion<textarea name="message" rows={6} placeholder="Occasion, location, family tradition or anything else that may help..."/></label><div className="full form-note">This interface intentionally does not publish a telephone number. Connect this form to your preferred email/CRM endpoint before production.</div><button className="btn full-button" type="submit">Submit enquiry</button></form></div></section></main></Shell>}