import type {Metadata} from 'next';
import Link from 'next/link';
import {Shell} from '../components';
import {formatMoney,pujas} from '../data/marketplace';
export const metadata:Metadata={title:'Book a Puja',description:'Browse puja services with verified Pujaris, transparent pricing and optional samagri in Odisha.'};
export default function PujasPage(){return <Shell><main><section className="catalogue-hero"><div className="wrap"><span className="kicker">Puja catalogue</span><h1>Find the right puja for your occasion</h1><p className="lead">Browse all {pujas.length} available pujas. Configured packages show pricing; other services are confirmed after we understand your requirements.</p><div className="filter-bar"><span className="filter-chip">All {pujas.length} pujas</span><span className="filter-chip">Configured packages</span><span className="filter-chip">Online available</span></div></div></section><section className="section"><div className="wrap catalogue-grid">{pujas.map(puja=><Link className="catalogue-card" href={`/pujas/${puja.slug}`} key={puja.slug}><span className="service-icon">ॐ</span><h2>{puja.name}</h2><p>{puja.short}</p><div className="catalogue-meta">{puja.duration&&<span>{puja.duration}</span>}{puja.modes.includes('ONLINE')&&<span>Online available</span>}{puja.samagri!==null&&<span>Samagri option</span>}</div><strong className="catalogue-price">{puja.from===null?'Price confirmed after details':`From ${formatMoney(puja.from)}`}</strong><span className="card-link">View details →</span></Link>)}</div></section></main></Shell>}
