import {notFound} from 'next/navigation';import PractitionerProfile,{practitionerMetadata} from '../../practitioner-profile';import {getPractitioner} from '../../../lib/practitioner-repository';export const dynamic='force-dynamic';export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params,profile=await getPractitioner('NUMEROLOGIST',slug);return profile?practitionerMetadata(profile,'numerologists'):{};}export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params,profile=await getPractitioner('NUMEROLOGIST',slug);if(!profile)notFound();return <PractitionerProfile profile={profile} basePath="numerologists"/>}
