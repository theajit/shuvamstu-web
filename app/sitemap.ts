import type {MetadataRoute} from 'next'; import {services} from './data/services'; export default function sitemap():MetadataRoute.Sitemap{const base='https://shuvamstu.com';return [{url:base},{url:`${base}/about`},{url:`${base}/enquiry`},{url:`${base}/book`},...services.map(s=>({url:`${base}/services/${s.slug}`}))]}
