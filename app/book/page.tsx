import {schedulingServices} from '../../lib/scheduling'; export const metadata={title:'Book a Service | Shuvamstu'}; export default function BookPage(){return <main className="section"><div className="wrap"><h1>Plan your spiritual service</h1><p className="muted">Choose a service and preferred date. Exact confirmation depends on provider availability and ritual requirements.</p><form action="/contact" method="get"><label>Service<select name="service" required><option value="">Choose a service</option>{schedulingServices.map(s=><option key={s.slug} value={s.slug}>{s.name}</option>)}</select></label><label>Preferred date<input name="date" type="date" required/></label><button className="btn" type="submit">Continue enquiry</button></form></div></main>}