import {notFound} from 'next/navigation';import PractitionerProfile,{practitionerMetadata} from '../../practitioner-profile';import {getPractitioner} from '../../../lib/practitioner-repository';export const dynamic='force-dynamic';export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params,profile=await getPractitioner('PANDIT',slug);return profile?practitionerMetadata(profile,'pandits'):{};}export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params,profile=await getPractitioner('PANDIT',slug);if(!profile)notFound();return <PractitionerProfile profile={profile} basePath="pandits"/>}
