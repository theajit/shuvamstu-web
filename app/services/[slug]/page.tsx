import {notFound} from 'next/navigation'; import Link from 'next/link'; import {Shell} from '../../components'; import {serviceBySlug,services} from '../../data/services';
export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=serviceBySlug(slug);return s?{title:s.title,description:s.description}:{}}
export default async function ServicePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const s=serviceBySlug(slug);if(!s)notFound();return <Shell><main><section className="page-hero" style={{backgroundImage:`linear-gradient(90deg,rgba(43,20,13,.88),rgba(43,20,13,.32)),url('${s.image}')`}}><div className="wrap"><span className="kicker light">{s.eyebrow}</span><h1>{s.title}</h1><p>{s.description}</p><Link className="btn btn-light" href={`/enquiry?service=${s.slug}`}>Enquire about {s.title}</Link></div></section><section className="section"><div className="wrap detail-grid"><div><span className="kicker">The experience</span><h2>Tradition, with clarity.</h2><p className="lead">{s.intro}</p></div><aside>{s.highlights.map(x=><div className="highlight" key={x}>✓ {x}</div>)}</aside></div></section><section className="soft section"><div className="wrap split"><div><span className="kicker">Planning this occasion?</span><h2>Start with the details you know.</h2></div><div><p>You do not need to know every ritual requirement before enquiring. Share the occasion and context first; the next steps can follow from there.</p><Link className="btn" href={`/enquiry?service=${s.slug}`}>Send an enquiry</Link></div></div></section></main></Shell>}